# Synthetic PDF demo workspace

- This fixture is synthetic. Do not add assessment material or identifying information.
- Treat `requirements.pdf`, `test/score-summary.test.mjs`, and `package.json` as protected inputs.
- Only edit `src/score-summary.mjs` to solve the task.
- A workspace extension may be created only in Full Access mode and only under this workspace's `.echo` directory.
- Run `npm test` after the change. A successful model statement is not evidence without the command result.
